<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dxj.mapper.CategoryMapper">
    <select id="AmountCategoryInfo" resultType="com.dxj.vo.AmountCategoryInfoVo" parameterType="string">
        SELECT a.amount, c.pay_type
        FROM t_ka_amount a
        LEFT JOIN t_ka_category c
        ON a.category_id = c.id
        WHERE a.create_time  LIKE CONCAT('%',#{dateValue},'%')
    </select>
</mapper>